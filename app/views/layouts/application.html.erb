<!DOCTYPE html>
<html>
  <head>
    <title>Rubox</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div id="wrapper">
      <% if (params[:controller] != "sessions" && params[:controller] != "users") %>

        <%  user = current_user %>
        <% if user %>
          <div id="nav">
            <div id="logo"></div>
            <div id="user">
              Bienvenido <%= user.name %> - <%= link_to "Salir", logout_path %>
            </div>
            <div id="menu">
              <ul>
                <li><%= link_to "Ver Proyectos", projects_path %></li>
                <li><%= link_to "Crear Nuevo", new_project_path %></li>
              </ul>
            </div>
          </div>
          <div id="main">
            <%= yield %>
          </div>
        <% else %>
          <div id="nav">
            <div id="logo"></div>
            <div id="user">
              <%= link_to "Crear usuario", signup_path %> - <%= link_to "Iniciar sesión", login_path %>
            </div>
          </div>
          <div id="main">
            <h1>Bienvenido a Rubox</h1>
            <p>Inicie session o cree un usuario para comenzar</p>
          </div>
        <% end %>
      <% else %>
        <div id="main">
          <%= yield %>
        </div>
      <% end %>
      <div>
      </div>
    </div>
  </body>
</html>
